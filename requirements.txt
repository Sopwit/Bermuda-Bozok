# === Runtime Dependencies ===
# These are defined in pyproject.toml under [project.dependencies].
# Install via:  pip install -e .
#
# === Dev / Test Dependencies ===
# Defined in pyproject.toml under [project.optional-dependencies] dev.
# Install via:  pip install -e ".[dev]"
#
# This file exists as a convenience reference and for CI environments
# that require a flat requirements file. To install everything at once:
#
#   pip install -e ".[dev]"
