# Use this workspace to collaborate
workspace:
  id: 9a0c53b5-7f70-4cb8-a132-18bfc339df2a

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ../postman/collections/WeatherWise API: 97d8f52a-5514-4f33-a318-1f0a7cba9a17

# All resources in the `postman/` folder are automatically registered in Local View.
# Point to additional files outside the `postman/` folder to register them individually. Example:
localResources:
  collections:
    - ../postman/collections/WeatherWise API
  environments:
    - ../postman/environments/Local.environment.yaml
  globals:
    - ../postman/globals/workspace.globals.yaml
